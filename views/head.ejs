<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SOL Faucet</title>
    <style type="text/css" media="screen">
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: monospace;
            font-size: 16px;
            color: #555;
            /* background-color: #7d84b2; */
            background-image: radial-gradient(circle, #d9dbf1, #c2c5e1, #abaed1, #9499c2, #7d84b2);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .section {
            flex-grow: 1;
            display: flex;
            align-items: center;
        }

        .container {
            max-width: 600px;
            /* margin: 50px auto; */
            padding: 20px;
            background-color: #d9dbf1;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h1 {
            /* margin: .5rem 0 0 0; */
            color: #222;
        }

        /* p {
            color: #555;
        } */

        form {
            margin-top: 20px;
            display: flex;
            flex-direction: row;
            flex-wrap: wrap;
        }

        form .form-control {
            width: 50%;
            padding: 5px;
            text-align: start;
        }

        input[type="text"],
        input[type="number"] {
            width: 100%;
            padding: .5rem;
            margin-top: .25rem;
            /* margin-bottom: 10px; */
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .toggle-container {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .toggle {
            position: relative;
            display: inline-block;
            width: 60px;
            height: 34px;
        }

        .toggle input {
            display: none;
        }

        .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #7d84b2;
            transition: .4s;
            border-radius: 34px;
        }

        .slider:before {
            position: absolute;
            content: "";
            height: 26px;
            width: 26px;
            left: 4px;
            bottom: 4px;
            background-color: #ffffff;
            transition: .4s;
            border-radius: 50%;
        }

        input:checked+.slider {
            background-color: #7d84b2;
        }

        input:checked+.slider:before {
            transform: translateX(26px);
        }

        button {
            padding: 10px 20px;
            background-color: #7d84b2;
            color: #ffffff;
            font-family: inherit;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
            width: 100%;
            display: block;
        }

        button:hover {
            background-color: #7d84b2eb;
        }

        .footer {
            padding: 0.25rem;
            font-size: .8rem;
            font-style: oblique;
            color: #ffffff;
            width: 100%;
            text-align: center;
        }

        .popup-container {
            display: none;
            position: fixed;
            align-items: center;
            justify-content: center;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            z-index: 999;
        }

        .popup {
            background-color: #fff;
            max-width: 600px;
            width: 100%;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
            text-align: center;
        }

        .popup h2 {
            margin-top: 0;
            color: #222;
        }

        .popup p {
            margin-bottom: 1rem;
        }

        #loader-wrapper {
            display: none;
            position: fixed;
            z-index: 9999;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            align-items: center;
            justify-content: center;
        }

        .loader {
            border: 8px solid white;
            border-top: 8px solid #555;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            animation: spin 2s linear infinite;
        }

        @keyframes spin {
            0% {
                transform: rotate(0deg);
            }

            100% {
                transform: rotate(360deg);
            }
        }
    </style>
</head>